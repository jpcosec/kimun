name: sldb
version: 0.1.0
exports:
  - name: sldb_document_payload
    schema: contracts/schemas/sldb_document_payload.schema.json
  - name: sldb_kgdb_semantic_export
    schema: contracts/schemas/sldb_kgdb_semantic_export.schema.json
consumes: []
compatibility:
  requires: {}
export_surface:
  package_name: sldb
  version: 0.1.0
  commands: {}
  aliases: {}
  contracts:
    sldb_document_payload:
      name: sldb_document_payload
      description: Structured document payload emitted from markdown persistence.
      schema_ref: contracts/schemas/sldb_document_payload.schema.json
      version: 0.1.0
    sldb_kgdb_semantic_export:
      name: sldb_kgdb_semantic_export
      description: Versioned semantic store payload for KGDB ingestion without scraping .sldb/runtime internals.
      schema_ref: contracts/schemas/sldb_kgdb_semantic_export.schema.json
      fixture_ref: contracts/fixtures/sldb_kgdb_semantic_export.v1.json
      version: 1
  procedures: {}
  surfaces: {}
