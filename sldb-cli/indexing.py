def index_text(text: str) -> list[float]:
    """
    Provides semantic indexing for a given chunk of text.
    In practice, this would call an LLM embeddings endpoint or a local model.
    """
    # Return dummy vector
    return [0.1, 0.2, 0.3, 0.4]
