[package]
name = "sldb-ffi"
version = "0.1.0"
edition = "2021"

[lib]
name = "sldb_ffi"
crate-type = ["cdylib"]

[dependencies]
pyo3 = { version = "0.22.0", features = ["extension-module"] }
sldb-core = { path = "../sldb-core" }
