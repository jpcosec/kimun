# Pills

Pills are reusable context documents for the sldb desk routine.

## Notes

- Keep active task-to-pill binding in task docs.
- Add temporary context here only when it affects execution safety or scope.

## Current Pills

- `pill-001`: keep SLDB CLI output tied to document contracts.
- `pill-002`: prevent CLI error and serialization drift.
