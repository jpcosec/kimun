# Drawer

Deferred desk work for sldb-refactor-worktree lives here until it is promoted into active execution.

- `tasks/` holds deferred bounded planning tasks.
- `features/` holds deferred macrotask planning surfaces before promotion.
- `tracks/` holds the intermediate planning layer that groups related features into promotable execution lanes before they are broken into active tasks.
