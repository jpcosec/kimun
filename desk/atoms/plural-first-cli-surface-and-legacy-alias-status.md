---
id: plural-first-cli-surface-and-legacy-alias-status
title: Plural-first CLI surface and legacy alias status
five_wh_one_plus: what
tags:
- system:sldb
- layer:cli
- topic:cli
provenance: /home/jp/proyectos/hum-ecosystem/tools/sldb/README.md
---

# Plural-first CLI surface and legacy alias status

## Answer

The primary public workflow is organized around `stores`, `models`, `docs`, `fields`, `sections`, `find`, and `ast`, while older singular or raw surfaces are legacy or deprecated.

## Supporting points

- The docs clearly steer users toward the plural-first command family.
- Legacy aliases still exist but are no longer the primary interface.
- CLI continuity in the refactor should preserve this public direction.

## Related atoms

### Constrains

- [constrains:: [[code-linting]]]

### 5WH1+ neighborhood

- This atom is typed by its `five_wh_one_plus` field and should be queried together with nearby `what`/`how`/`when`/`where` atoms rather than as an isolated note.
