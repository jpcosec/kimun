---
id: ritual-phase
steps:
- verify the dependency layer is actually closed
- confirm every closed task has its contract artifact named in `files:`
- confirm downstream tasks only consume frozen contracts
- record the next dependency layer before new execution starts
tags:
- workspace:desk
---

# Phase ritual

Use this ritual when a whole planning layer is closed and the board is ready to move to the next dependency layer.
