# Lane A — internal coherence of docs/v2 + epoch:v2 atoms (round 1)

Model: claude-haiku-4-5 · type: Explore (read-only) · fresh context · 2026-08-29

## Review

**Correct:**
- Node immutability and content-addressed identity by hash (02:2, 02:10.1-2, 01:§4.3) — identity rule is trivial and exact
- Trees as index collections of ownership edges; one node per tree parent (02:§3, 02:10.4-5, atom-tree-as-index) — well specified
- S/M/G layer distinction as node classes over shared pool (01:§3, 02:§2, atom-smg-as-persisted-layers) — clearly articulated
- Reversibility scope: S↔CST reversible by construction; S→M deterministic but context-dependent; M→S is generation not inversion (01:§5, atom-reversibility-scope-in-v2) — boundaries are clear
- Succession via supersedes edge; identity across edits is recorded, not inferred (02:§6, atom-identity-is-the-content-hash) — well specified
- Anchor states: intact/superseded/drifted/orphan with evidence comparison (02:§6, atom-evidence-edges-and-anchor-states) — fully defined
- Determinism as authority (01:§4.6, atom-canonical-projected-or-asserted) — canonical/projected/asserted distinction is clean
- Pure cljc kernel with hosts as adapters (02:§8, atom-decision-pure-cljc-kernel-with-hosts-as-adapters) — rationale and consequences clear
- Roadmap as Git object model vertical slices (02:§9, atom-git-object-model-as-the-v2-roadmap) — analogies well mapped through milestones 0-8

- Note [high]: docs/v2/02-sustrato-computacional.md:116-122 — TransactionPlan is "dados EDN: [primitive-op ...]" with operations add-node/add-edge/remove-edge/replace/move listed, but concrete EDN schema (field names, types, required/optional) never specified. — What is the exact EDN structure of each primitive operation (e.g., does add-edge require evidence for binding edges, or only for cross-layer)?
- Note [high]: docs/v2/02-sustrato-computacional.md:2, section 2 — Node has `content: datos canónicos del nodo` but canonical form differs by class: for S nodes (text leaves), is it UTF-8 bytes alone or with offset markers? For M nodes (propositions/symbols), what logical form exactly? For G nodes (triples), is it (subject-id, predicate-id, object-id) or concrete values? — How is canonical content defined per node class such that hash(content) uniquely identifies it?
- Note [high]: docs/v2/02-sustrato-computacional.md:63, section 3, ownership edge table — Tree Merkle hashes "solo esas aristas" but ownership edges carry `order: posición entre hermanos` (line 56). Does sibling order participate in Merkle computation? Are siblings hashed as a sequence or individually? — How are sibling order and parent-child edges combined in canonical form for Merkle hashing?
- Note [high]: docs/v2/02-sustrato-computacional.md:128-129, section 5 — "conflicto → reconciliación por árbol ... o `ConflictSet` explícito" but ConflictSet is never defined. — What is ConflictSet's structure, how are conflicts detected (same edge type, different targets?), and what resolution semantics apply?
- Note [high]: docs/v2/02-sustrato-computacional.md:138-140, section 6 — Superseded node: "Las aristas colgadas de `X` se re-anclan a `X'` por regla explícita (según tipo) o quedan marcadas para revisión." But no table of per-type re-anchoring rules exists. — For each edge type (binding, reference, semantic, derived), what happens when its source or target is superseded: auto-reroute to successor, leave dangling, or mark for review?
- Note [high]: docs/v2/02-sustrato-computacional.md:2, section 2, and section 3 edge table — M nodes are "proposiciones en forma canónica" but the Node struct shows only generic class/kind/content/address. Compared to G nodes which are "(sujeto, predicado, objeto)", what structure does M node content have? — Does an M node store (subject-symbol, predicate-symbol, object-symbol) like G, or something different (e.g., a lambda term, a first-order formula)?
- Note [high]: docs/v2/02-sustrato-computacional.md:84-103, section 4 — Stand-off canonical leaf is "texto plano de cada párrafo con offsets de grafema" but exact canonical form unclear: is content just {text: "..."} and offsets stored elsewhere, or {text: "...", offsets: [...]}, or something else? Further, line 99 states graphemes cannot be nodes. — When a symbol is anchored at (leaf-hash, offset-range), is that address virtual (computed on demand) or does a span node materialize in the pool?
- Note [high]: docs/v2/02-sustrato-computacional.md:44, section 2, and 57 edge evidence — Evidence is "{hash-del-referente, actor|motor, versión, contexto W_i, timestamp}" but W_i is mentioned repeatedly (01:36, 02:44, 52, 57, 218) and never defined as a data structure. In hito 7 "adaptador Matrix", W_i is implied to be a Matrix construct. — What is W_i: is it a tree, a node id, an external reference? How many W_i per document? How does binding know its W_i context?
- Note [medium]: docs/v2/02-sustrato-computacional.md:130-131, section 5 — "Efectos externos ... salen como `EffectPlan` a un outbox persistente" but EffectPlan is never defined. — What fields does EffectPlan contain, and how does it reference the external operation (git command, filesystem write, API call)?
- Note [medium]: docs/v2/01-orden-filosofico.md:69-70, section 4, and 02:56 edge struct — "Un nodo tiene un padre *por árbol*, no un padre global" but edges are stored separately from nodes. Query: "in how many trees is this node?" requires scanning all edges. — Is there a reverse index (node → edges containing it) materialized in the kernel, or computed on demand?
- Note [medium]: docs/v2/02-sustrato-computacional.md:64, section 3 binding table — Binding edge: "este signo expresa este símbolo, en este contexto" but if the same sign maps to different symbols in different W_i (01:89-90, "gift" in English vs German), is there one binding edge with evidence.W_i, or separate edges per W_i? — How are multi-context bindings represented and queried?
- Note [medium]: docs/v2/02-sustrato-computacional.md:18, section 1, and 127-129 — "document head y heads de índice" with CAS merge, but Revision struct (line 110) lists `roots: {tree-id → merkle-root}`, not heads. — Are heads stored separately (where?), and can there be multiple competing heads per tree/document (e.g., from concurrent edits)?
- Note [medium]: docs/v2/02-sustrato-computacional.md:40-41, section 2 — Opaque nodes are "blob + formato de origen" with hashing, but Node.content for opaque is not specified: is it hash(blob), or (hash(blob), format-string)? Invariant 12 (line 241) says opaque operations replace the blob; if canonical is hash only, how is drift detected? — What is the canonical content representation for opaque nodes?
- Note [medium]: docs/v2/02-sustrato-computacional.md:79-80, section 3 — Merkle is "perezoso" (lazy) and "se recalcula al consultar, exportar o confirmar revisión" but tree representation is not specified: adjacency list, cell-with-children, or other? — How is a tree stored and what does "recompute Merkle" mean operationally given the representation?
- Note [medium]: docs/v2/02-sustrato-computacional.md:65, section 3 projection table — Projection: "este símbolo se consolidó en este hecho" and hito 7 mentions "consolidación validada" but validation criteria not specified. — What does it mean for a symbol to consolidate into a fact; is it identity-of-fields, or a separate validation gate?
- Note [medium]: docs/v2/02-sustrato-computacional.md:7, introduction — "aristas que cruzan capas (S→M→G) con evidencia" yet evidence struct (line 57) lists evidence fields. For edges within a layer (reference S↔S, semantic M↔M), does evidence still carry all five fields or a subset? — Which edge types require evidence and which fields are mandatory vs optional?
- Note [low]: docs/v2/02-sustrato-computacional.md:241, section 10 invariant 12, and section 5 line 122 — "Toda operación sobre un nodo :opaque es reemplazo del blob completo, o se rechaza" but primitives list shows replace/move as separate from add-node. — Is replace-whole-opaque-blob a special case of replace, or its own primitive?
- Note [low]: docs/v2/02-sustrato-computacional.md:215, section 9 hito 4, vs line 166 section 7 — Hito 4 criterion: "parse(render(A)) == A por property testing"; section 7 invariant: "parse_f(render_f(A)) == A para A canónico". Is hito 4 weaker (only render-idempotence) or does canonical form happen earlier? — Should hito 4 criterion also require A to be canonical, or is canonicalization tested separately?

## Verdict

Counts: high=8 medium=11 low=3 (as reported by the lane; 8 high, 8 medium and 2 low lines are listed above — the lane's own count of mediums/lows over-states its list by 3/1).

The bundle is NOT coherent enough to begin milestone 0 without resolving the high-severity gaps. Milestone 0 requires "hash canónico" but canonical content forms for M nodes, G nodes, opaque nodes, and stand-off leaves are not defined — meaning hash identity cannot be implemented. TransactionPlan's EDN schema is missing, blocking transaction serialization. Re-anchoring rules for superseded edges are absent. W_i context structure must be defined before binding or projection edges can be materialized. Merkle sibling ordering is unspecified, making milestone 1 (tree verification) impossible.
