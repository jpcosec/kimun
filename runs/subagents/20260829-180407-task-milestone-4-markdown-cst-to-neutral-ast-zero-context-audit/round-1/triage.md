# Triage — milestone 4 gate, round 1
A: 8 high / 3 medium / 2 low · B: 3 high / 2 medium / 2 low — all spec precision, no design decisions returned to the user.
Actions: docs/v2/04 rewritten with closed rules — §2 TextSegmenter returns a vector of grapheme strings; §3.1 regex table for block starts and explicit continuation/closing rules (lists by marker width w, blank-line handling, lazy continuation; quotes cut by blank; fences close only with the same char and ≥ length; html/table until blank); §4 canonical :attrs per type, mark model with sort key [s (- e) kind], nesting rules; §4.1 canonical AST set for gen-ast; §5 one-pass inline algorithm with delimiter stack, unescape rule, every :unparsed condition; §6 render with the exact escape set and line-start rule, fence length, list widths; §7 opaque formats; §8 ast->plan / store->ast / markdown->plan / store->markdown signatures; §9 coverage definition; §10 promises rows with test names. 02 §4 corrected (inline layer is stored on the block, not derived) and §2.1 example updated. promises.md points to 04 §10.
Round 2: relaunch A and B.
