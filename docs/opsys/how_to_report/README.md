# How To Report

The canonical home for this guide is now the sibling `deskops` repo.

Use:

- `../deskops/docs/how-to-report.md` in a local sibling checkout

This local copy is now only a forwarding note so `sldb` stops being the long-term home of generic workflow guidance.
