;; Release matrix for `bb release` (docs/v2/08 §5). `:version` must equal
;; resources/VERSION. `:sha256` digests of the babashka assets are nil until
;; pinned with `bb release --pin`; a nil digest aborts a non-local build.
{:version "2.0.0-alpha.1"
 :bb "1.13.219"
 :platforms [:linux-amd64 :linux-aarch64 :macos-amd64 :macos-aarch64 :windows-amd64]
 :sha256 {:linux-amd64 nil
          :linux-aarch64 nil
          :macos-amd64 nil
          :macos-aarch64 nil
          :windows-amd64 nil}}
